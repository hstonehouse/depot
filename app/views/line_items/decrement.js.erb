cart = document.getElementById("cart")
cart.style.display = ""
cart.innerHTML = "<%= j render(@cart) if @cart %>"

<%# j is an alias for escape_javascript()
which converts the Ruby string into a format acceptable as input to JS
everything except the erb executes on the browser %>

notice = document.getElementById("notice")
if (notice) notice.style.display = "none"

<%# if there is a notice when you create a new line item, hide it %>